# P1: Agent 自主性约定 (档 2 PR 模式) — 245 测绿

## 背景

per master roadmap (`docs/superpowers/specs/2026-05-06-master-roadmap-design.md`) §6, Scott 在 brainstorm Q3 选定**档 2 PR 模式**. 需要把这一约定从对话沉淀成 `.claude/AUTONOMY.md` 元规则文档, 让所有未来 session / 新 agent 都按统一规则跑.

## 改动

### 新文件

1. **`.claude/AUTONOMY.md`** (~120 行) — 档位定义 / 自动允许 / 要授权矩阵 / PR 自审 checklist / 升降档触发
2. **`tests/test_autonomy_doc_invariants.py`** (9 测) — 文档守护, 防关键约束被未来无意改坏

### 修改

3. **`CLAUDE.md`** (+5 行) — 加索引节, 让 auto-loaded 协作约定能跳到 AUTONOMY
4. **`.gitignore`** — 由"忽略整个 `.claude/`"改为"只忽略 `settings.local.json` + `scheduled_tasks.lock`"; 让 `AUTONOMY.md` 能进 git

### Memory 同步 (跨项目 home, 不进本仓库 git)

- `feedback_deploy_skill_push_authorization.md` 顶部加 D4 部分放松声明: push feature 分支不再问; push main / `--force` / deploy / 花钱 仍要授权; 歧义即不动

## 验证

- [x] 全测 `python -m pytest -q` → **245 passed, 1 skipped, 0 failed** (基线 236 + 9 新守护)
- [x] 9 守护测覆盖:
  - 档 2 PR 模式声明
  - 禁 push main
  - merge 要授权
  - deploy 要授权
  - 花钱要授权
  - 自审 checklist 含 pytest / smoke / PR description
  - 升降档数值阈值 (≥80%, ≤2 行 等)
  - CLAUDE.md 索引存在
- [x] grep `.claude/AUTONOMY.md` 在 CLAUDE.md 命中 → AUTO-loaded 协作约定能跳到本文档
- [x] grep "档 2" / "PR 模式" / "授权" 关键短语全部在 AUTONOMY.md 出现

## 风险

零业务代码改动, 零运行时影响. 风险只在文档措辞 — 等本 PR review 时 Scott 校对升降档阈值是否合理.

## 回滚

```bash
git revert <merge-commit>
```

回滚后回到档 0 (现状), 所有 push/merge/deploy 都需 Scott 显式授权. 守护测也会一并回滚.

## 留作后续 PR (本次 deferred)

- `.claude/settings.json` / `agents/` / `hooks/` / `skills/` 现在 untracked 但不再被忽略 (因 .gitignore 改). 这些是项目级 config (per AUTONOMY.md 自己说"团队规则永远写 settings.json"), 应当进 git, 但塞进本 PR 会扩大 scope. 建议独立 follow-up PR `feat/p1-1-claude-config-publish` 处理.

## Done Criteria 对照 (master roadmap §12)

- [x] `.claude/AUTONOMY.md` 存在, 含 7 节
- [x] CLAUDE.md grep 到 `.claude/AUTONOMY.md` 链接
- [x] memory `feedback_deploy_skill_push_authorization.md` 含 "D4 部分放松" 字样
- [x] 全测 ≥ 245 passed
- [x] PR 已开
- [ ] PR merge → 等 Scott

🤖 Generated with [Claude Code](https://claude.com/claude-code)
