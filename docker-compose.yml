services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      # API Key 由各用户在「账号设置」中自行配置，无需平台环境变量
    volumes:
      - uploads:/app/static/uploads
      - outputs:/app/static/outputs
    restart: unless-stopped

volumes:
  uploads:
  outputs:
